name: ctrim_app
description: A new Flutter project.

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

# version: 0.6.1+13 # iOS
version: 0.6.2+13 # Android
# version: 0.6.1+13 # Web

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  add_2_calendar: 
  avatar_stack: 
  cloud_firestore: 
  cloud_functions: 
  firebase_analytics: 
  firebase_auth: 
  firebase_core: 
  firebase_messaging: 
  firebase_messaging_web: 
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_quill: 
  flutter_quill_extensions:
  http: 
  intl: 
  package_info_plus: 
  path_provider: 
  photo_view: 
  provider:
  shared_preferences: 
  url_launcher: 
  video_player:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    # Add assets from the images directory to the application.
    - assets/images/
    - assets/info/
    - assets/info/one_verse/
    - assets/info/topic/
