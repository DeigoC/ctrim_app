name: ctrim_app
description: A new Flutter project.

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

# version: 0.6.1+13 # iOS
# version: 0.6.1+12 # Android
version: 0.6.1+13 # Web

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  # chewie: ^1.5.0
  add_2_calendar: ^3.0.1
  avatar_stack: ^1.2.0
  cloud_firestore: ^4.9.2
  cloud_functions: ^4.4.1
  firebase_analytics: ^10.5.0
  firebase_auth: ^4.10.0
  firebase_core: ^2.16.0
  firebase_messaging: ^14.6.8
  firebase_messaging_web: ^3.5.7
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_quill: ^7.4.7
  flutter_quill_extensions: ^0.4.1
  http: ^0.13.6
  intl: ^0.18.0
  package_info_plus: ^4.1.0
  path_provider: ^2.1.1
  photo_view: ^0.14.0
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.14
  video_player: ^2.7.2
  video_thumbnail: ^0.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    # Add assets from the images directory to the application.
    - assets/images/
