name: ctrim_app
description: A new Flutter project.

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 0.8.4+31 # iOS
# version: 0.8.3+27 # Android

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  avatar_stack: 
  cloud_firestore: 
  cloud_functions: 
  firebase_analytics: 
  firebase_app_check: 
  firebase_auth: 
  firebase_core: 
  firebase_messaging: 
  firebase_messaging_web: 
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_quill:
  http: 
  intl: 
  package_info_plus: 
  path_provider: 
  photo_view: 
  provider:
  share_plus: ^11.1.0
  shared_preferences: 
  url_launcher:
  universal_html: ^2.2.4
  video_player:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:
  mocktail:

flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    # Add assets from the images directory to the application.
    - assets/images/
    - assets/info/testimonials/
    - assets/info/ctrim_info/
    - assets/info/churches/
    - assets/personal/
